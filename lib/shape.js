// Define a Shape class for managing logo attributes
class Shape {
    constructor(shapeColor, text, textColor) {
        // Initialize shape color, text, and text color
        this.shapeColor = shapeColor;
        this.text = text;
        this.textColor = textColor;
    }
};

module.exports = Shape;